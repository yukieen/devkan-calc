apply plugin: 'base'

task wrapper(type: Wrapper) {
  gradleVersion = '1.5'
}

subprojects {
  apply plugin: 'java'
  
  sourceCompatibility = 1.7
  targetCompatibility = 1.7

  repositories {
    mavenCentral()
  }
}
