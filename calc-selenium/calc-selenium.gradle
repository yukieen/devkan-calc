dependencies {
    testCompile 'junit:junit:4.11'
    testCompile 'org.seleniumhq.selenium:selenium-java:2.32.0'
}

task localTest(type:Test) {
  def app = project(':calc-app')
  doFirst { app.jettyRun{ daemon=true; execute() } }
  doLast { app.jettyStop.execute() }
}
